<UserControl x:Class="Formula81.XrmToolBox.Libraries.Parts.Controls.FilterInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Formula81.XrmToolBox.Libraries.Parts.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="24" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="FilterTextBox" FontSize="12" VerticalContentAlignment="Center" Margin="0,0,2,0" 
                 TextChanged="FilterTextBox_TextChanged" />
        <Button x:Name="ClearFilterButton" Grid.Column="1" Content="❌" FontSize="8" />
        <TextBlock IsHitTestVisible="False" Text="Filter..." Foreground="Gray" Margin="5,0,0,0"
                               VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text, ElementName=FilterTextBox}" Value="">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
